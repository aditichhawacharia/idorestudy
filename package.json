{
  "name": "idorestudy-adsense-ready",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs",
    "preview": "vite preview",
    "audit": "node scripts/syntax-check.mjs && node scripts/content-audit.mjs",
    "audit:prelaunch": "npm run audit && node scripts/prelaunch-audit.mjs",
    "check": "npm run audit && npm run build",
    "check:prelaunch": "npm run audit:prelaunch && npm run build",
    "build:client": "vite build"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.1.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.5",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
